
# Enforce LF (Unix) line endings for text files
* text=auto eol=lf

# Keep large/binary files managed by Git LFS and do not normalize their line endings
*.pt filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
